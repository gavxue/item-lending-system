export default function Admin() {
    return (
        <div>
            <h1>Admin</h1>
            {/* <table className="table fw-normal">
                <thead>
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col">Item</th>
                        <th scope="col">Name</th>
                        <th scope="col">Email</th>
                        <th scope="col">Date Loaned</th>
                    </tr>
                </thead>
                <tbody>
                    {data && data.map((entry) => (
                        <tr key={entry.id}>
                            <th scope="row">{entry.id}</th>
                            <th className="fw-normal">{entry.item}</th>
                            <th>{entry.name}</th>
                            <th>{entry.email}</th>
                            <th>{entry.date_loan}</th>
                        </tr>
                    ))}
                </tbody>
            </table> */}
        </div>
    )
}